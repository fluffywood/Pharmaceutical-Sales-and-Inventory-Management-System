<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.PurchaseReporterMapper">

    <select id="selectAll" resultType="com.example.entity.PurchaseReporter">
        select `purchase_reporter`.* from `purchase_reporter`

        <where>

        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.PurchaseReporter">
        select * from purchase_reporter where id = #{id}
    </select>

    <delete id="deleteById">
        delete from purchase_reporter where  id = #{id}
    </delete>

</mapper>